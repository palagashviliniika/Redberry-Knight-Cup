import React from 'react';
import {Container, Logo} from "../Components/LandingElements";
import Navigation from "../Components/Navigation";

const Landing = () => {
    return (
        <Container>
            <Navigation />
        </Container>
    );
};

export default Landing;